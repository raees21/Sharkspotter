# SharkSpotter Frontend

## Getting Started

In the project directory, you can run

1. Install the required modules

    ```bash
    npm i
    ```

2. Start the app in development mode:

    ```bash
    npm start
    ```

Open [http://localhost:3000](http://localhost:3000) to view it in your browser.